program P4;

{$APPTYPE CONSOLE}

uses  SysUtils,
      P4Fun in 'P4Fun.pas';

var a:array[1..10] of integer;
    i,n:integer;

begin
  Write('Input n:');
  ReadLn(n);
  for i:=1 to n do
    Read(a[i]);
  ReadLn;
  WriteLn('Sum=',sum(a,n));
  ReadLn;

end.
